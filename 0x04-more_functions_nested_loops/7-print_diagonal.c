#include "main.h"
/**
* print_diagonal - print a diagonal line
* @n: integer value
*/
void print_diagonal(int n)
{
	int v, u;

	if (n <= 0)
		_putchar('\n');
	else
	{
		for (v = 0; v < n; v++)
			for (u = 0; u < v u++)
				_putchar(' ');
		_putchar(92);
		_putchar('\n');
	}
}
