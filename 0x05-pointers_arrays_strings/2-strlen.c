#include "main.h"
#include <string.h>
/**
* _strlen - function that prints the length of a string
* @s: string that passed to this function
* Return: the length of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
